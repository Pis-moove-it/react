import styled from 'styled-components';

const BoxTitle = styled.a`
  font-family: Candara;
  line-height: normal;
  font-size: 35px;
  text-align: center;

  color: #0797BA;

  flex: 2;
`;

export default BoxTitle;
