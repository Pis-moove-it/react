import styled from 'styled-components';

const Question = styled.a`
  font-family: Candara;
  line-height: normal;
  font-size: 22px;

  text-align: center;

  color: #0797BA;

  flex: 1;
`;

export default Question;
