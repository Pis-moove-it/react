import styled from 'styled-components';

const BoxInfo = styled.div`
  font-family: Candara;
  line-height: normal;
  font-size: 20px;

  color: #000000;

  flex: 1;

  display: flex;
  flex-direction: row;
`;

export default BoxInfo;
