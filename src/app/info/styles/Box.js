import styled from 'styled-components';

const Box = styled.div`
  font-family: "Candara";
  font-weight: bold;

  display: flex;
  flex-direction: column;

  height: calc(150vh - 55px);
`;

export default Box;
