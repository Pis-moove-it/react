import styled from 'styled-components';

const BodyWrapper = styled.div`
  position: relative;
  top: 55px; /* begin body after the header */
  display: flex;
  flex-direction: column;
`;

export default BodyWrapper;
